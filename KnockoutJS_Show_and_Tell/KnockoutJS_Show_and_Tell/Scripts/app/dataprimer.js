define('dataprimer',
    ['citydata', 'weatherdata'],
    function (citydata, weatherdata) {

        var fetch = function () {
        };


        return {
            Fetch: fetch
        };
    }
);